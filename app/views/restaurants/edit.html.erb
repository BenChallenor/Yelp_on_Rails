<h1>Edit a restaurant</h1>

<%= render 'form' %>

<p> <div id="delete"><%= link_to "Delete restaurant", restaurant_path(@restaurant),
          method: :delete,
          data: { confirm: "Are you sure?" } %>

<p><%= link_to "Back to restaurant listings", restaurants_path%></div>
